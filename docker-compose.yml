version: '3'
#services:
#	server:
#		image: golang
#		#build: ./app
#		environment:
#			APP_SERVER_PORT: ${APP_SERVER_PORT}
#		expose: 
#			- ${APP_SERVER_PORT}
#		ports:
#			- ${APP_SERVER_PORT}:${APP_SERVER_PORT}
#		command: ./app/bin/msg
#	
#	client:
#		image: react
#		#build: ./client
#		environment:
#			- REACT_APP_PORT=${REACT_APP_PORT}
#		expose:
#			- ${REACT_APP_PORT}
#		ports:
#			- ${REACT_APP_PORT}:${REACT_APP_PORT}
#		links:
#			- server